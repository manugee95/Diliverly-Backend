export enum DisputeStatus {
  OPEN = 'open',
  IN_REVIEW = 'in_review',
  WAITING_VENDOR = 'waiting_vendor',
  WAITING_AGENT = 'waiting_agent',
  RESOLVED = 'resolved',
  REJECTED = 'rejected',
  CLOSED = 'closed',
}