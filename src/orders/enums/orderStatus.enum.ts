export enum OrderStatus {
  IN_PROGRESS = 'in_progress',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  COMPLETE = 'completed',
  PENDING = 'pending',
  ACTIVE = 'active',
  DECLINED = 'declined',
  OVERDUE = "overdue",
  DISPUTE = "dispute"
}